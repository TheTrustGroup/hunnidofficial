# Documentation

Active references:

- [API integration](./API_INTEGRATION.md) — endpoint overview (prefer `POST /api/sales` for POS)
- [Performance](./PERFORMANCE_OPTIMIZATIONS.md) — caching and deploy notes

Stale root-level markdown was moved to `../_archive/docs-legacy/`.
