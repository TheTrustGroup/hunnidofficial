# Archived duplicate trees

Moved on consolidation so **all product work happens in `warehouse-pos/`** only.

| Folder | Was |
|--------|-----|
| `legacy-root-src/` | `/src` at repo root |
| `legacy-root-inventory-server/` | `/inventory-server` at repo root |
| `World-Class-Warehouse-Inventory-Smart-POS-System/` | Nested duplicate project |

Do not edit these paths for new features. Refer to git history here if you need an old diff.
